{% extends "_layout" %}
{% block main %}
		{% block header %}

<div class="pages-header">

	<div class="content-wrapper">
			<div class="dropdown">
				<div class="dropbtn">▼</div>
				<div class="dropdown-content">
					{% for entry in craft.entries.section("pages").all() %}
						<a href="{{entry.url}}">
							<div class="h1">
								{{entry.rubrik}}
							</div>
						</a>
					{% endfor %}
				</div>
			</div>


			{% for entry in craft.entries.section('header').all() %}

				<div class="pages-hrubrik" style='color:white'>
					{{entry.rubrik}}
				</div>

			{% endfor %}
	</div>





</div>
		{% endblock %}
	<div class="page-nav">

		{% set criteria = craft.entries.section('pages') %}

		{% set prev = entry.getPrev(criteria) %}
		{% set next = entry.getNext(criteria) %}



			{% if prev %}
				<div class="prev">
				<img  src="../ikoner/prev.svg" href="{{ prev.url }}"></a>
				</div>
			{% endif %}


			{% if next %}
				<div class="next">
				<img  src="../ikoner/next.svg" href="{{ next.url }}"></a>
				</div>
			{% endif %}




		<div class="page-content-wrapper">
			{% set assets = entry.ikon %}
			{% if assets.Url is defined %}
				{% for item in assets %}
					<img class="pages-ikon" src="{{ item.Url }}">

				{% endfor %}
			{% endif %}
				<div class="pages-rubrik">
					{{entry.rubrik}}
				</div>

				<div class="brodtext">
					{{ entry.brodtext}}
				</div>


			<div class="clearfix">
				{% set criteria = craft.entries.section('pages') %}
				{% set prev = entry.getPrev(criteria) %}
				{% set next = entry.getNext(criteria) %}

					{% if prev %}
						<a class="button" href="{{ prev.url }}">Tidigare</a>
					{% endif %}

					{% if next %}
						<a class="button" href="{{ next.url }}">Nästa inlägg</a>
					{% endif %}

					{% if next %}
						<a class="button" href="/">Hem</a>
					{% endif %}


			</div>
		</div>

	</div>
{% endblock %}
